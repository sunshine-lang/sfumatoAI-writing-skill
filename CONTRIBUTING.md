# Contributing

感谢你改进 sfumatoAI Writing Skill。

## 提交原则

1. 保持 Skill 的核心顺序：先搜索核验，再提出技术选题。
2. 技术定义、时效性事实和能力边界必须附可靠来源，优先官方文档与原始论文。
3. 不把营销话术、社交媒体截图或单一二手文章直接写成事实。
4. 不提交无权公开的人物参考图、截图、字体、品牌素材或用户私有内容。
5. 保持 `SKILL.md` 精简；详细规范放入 `references/`，输出模板放入 `assets/`。
6. Skill 内部标识固定为 `sfumatoai-writing-skill`，不要改成含大写字母的名称。

## 开发流程

1. Fork 本仓库并创建用途明确的分支。
2. 只修改与本次贡献相关的文件。
3. 运行仓库检查：

   ```bash
   python3 scripts/check_repository.py
   ```

4. 若修改 `validate_delivery.py`，至少使用一份有效清单和一份无效清单验证成功与失败路径。
5. 在 Pull Request 中说明修改内容、原因、行为影响、验证方法和来源。

## 内容变更

新增或修改概念案例时，请同时说明：

- 概念范围；
- 目标受众；
- 一手来源；
- 容易误解的说法；
- 推荐的安全表述；
- 是否改变既有工作流或交付契约。

## 安全与隐私

- 不提交 API Key、登录凭据、Cookie、个人隐私或未公开文件。
- 示例内容应去除可识别个人或组织的信息。
- 对不确定的素材授权保持保守：只保留文字规范，不上传原始素材。
